services:
  - type: web
    name: astock-recommend-mobile
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: MOBILE_ONLY
        value: "1"
      - key: ADMIN_PASSWORD
        generateValue: true
